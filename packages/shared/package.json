{
  "name": "@placement-prep/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for PlacementPrep",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest --run"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vitest": "^0.34.4"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}